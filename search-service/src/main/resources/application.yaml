spring:
  application:
    name: search-service

  datasource:
    url: jdbc:postgresql://postgres:5432/search_db
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  jpa:
    hibernate:
      ddl: validate

  liquibase:
    enabled: true
    change-log: db/changelog/db.changelog-master.xml
